
    <%= @user.igId %>, <%= @user.name %>, Total follow: <%= @folNum %> 
    
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <%= form_for(:session, url: "/getfollower") do |f| %>

          <%= f.label 'Target User Name' %>
          <%= f.text_field :targetusername 
          %>

          <%= f.submit "Get follower", class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>